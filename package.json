{
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "scripts": {
    "package:indices": "cd indices && npx @vercel/ncc build index.js -o dist --license licenses.txt",
    "package:defaults": "cd defaults && npx @vercel/ncc build index.js -o dist --license licenses.txt",
    "package:all": "npm run package:defaults && npm run package:indices"
  }
}
